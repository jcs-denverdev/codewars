// Description:
// What if we need the length of the words separated by a space to be added at the end of that same word and have it returned as an array?

// Your task is to write a function that takes a String and returns an Array/list with the length of each word added to each element .

// Note: String will have at least one element; words will always be separated by a space.

//Params
// string param
//Returns
// return an array with the length of each word added to each element
//Examples
    // "apple ban" --> ["apple 5", "ban 3"]
    // "you will win" -->["you 3", "will 4", "win 3"]
//Pseudocode
// split string of potentially multiple words into an array containing each word str.split(" ")
// map() creates a new array populated with results of calling provided function on every element in the calling array

function addLength(str) {

    // str.split(" ")

}

console.log(addLength("apple ban"));